server.port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/sanjuthomas/config-store
          search-paths:
            - jobs-service
            - news-service
            - profiles-service
            - attorneys-service
          default-label: develop